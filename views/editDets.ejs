<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        body{
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        textarea{
            border-radius: 20px;
            border: 2px solid black;
            padding: 0.5rem;
            font-size: 15px;
            width: 100%;
            text-align: center;
            resize: none;
        }
        input{
            border: 2px solid black;
            border-radius: 20px;
            background-color: #fff;
            height: 3rem;
            width: 100%;
            font-size: 15px;
            padding: 0 10px;
            text-align: center;
        }
        button{
            height: 2.5rem;
            width: 4rem;
            background-color: #000000;
            border: 2px solid black;
            border-radius: 15px;
            color: #fff;
            font-size: 15px;
        }
        button:hover{
            cursor: pointer;
        }
        * {
            font-family: 'Poppins';
            /* margin: 0;
            padding: 0;
            box-sizing: border-box; */
        }
        form{
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            width: fit-content;
            gap: 1rem;
        }
        .editing{
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: #40ff6a;
            width: 20%;
            height: max-content;
            border-radius: 20px;
            padding-bottom: 0.5rem;

        }
        p{
            font-weight: 700;
            font-size: 18px;
            color: red;
        }
    </style>
    <title>Edit</title>
</head>

<body>

    <div class="editing">
        <h2>Edit Details</h2>
        <form method="POST" action="/user/<%=user.userId%>?_method=PATCH">
            <textarea name="username" cols="25" rows="2"  ><%= user.username %></textarea>
            <textarea name="email" cols="25" rows="2"  ><%= user.email %></textarea>
            <input type="password" name="password" placeholder="Enter Password">

                <% if(noPass){  %>
                    <p>WRONG PASSWORD</p>
                    <% } %>

            <button>EDIT</button>
        </form>
    </div>

</body>

</html>